DECLARE 
	Z NUMBER;
FUNCTION DIFF(A IN NUMBER, B IN NUMBER)
RETURN NUMBER 
IS 
	C NUMBER; 
BEGIN 
	IF A > B THEN
		C := A - B;
	ELSE 
		C := B - A;
	END IF;
	RETURN C;
END ;

BEGIN
	Z := DIFF(10,25);
	DBMS_OUTPUT.PUT_LINE('THE DIFFERENCE IS: '||Z);
END;
/
